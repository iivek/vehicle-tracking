// Class that implements an input queue with a worker thread that manages sample delivery
// to a worker thread that does the real work inside the filter
// i.e. input pin delivers the stream to our input queue who passes it forward for processing

